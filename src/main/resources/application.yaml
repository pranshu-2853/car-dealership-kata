spring:
  application:
    name: car-dealership

app:
  cors:
    allowed-origins: http://localhost:5173
  jwt:
    secret: ${JWT_SECRET:local-development-secret-key-change-me-in-production-please}
    expiration-ms: 3600000
